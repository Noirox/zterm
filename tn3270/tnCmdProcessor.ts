export class CommandProcessor {

  processData(bytes: number[], length: number) {

  }

  processRecord(bytes: number[], length: number) {

  }

  processCommand(byte: number[], length: number) {

  }

  processSubcommand(byte: number[], length: number) {

  }
}
